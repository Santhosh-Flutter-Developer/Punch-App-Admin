import 'package:flutter/material.dart';

class Mod {
  final String label, route;
  final int count;
  final IconData icon;
  final Color color;
  const Mod(this.label, this.count, this.icon, this.color, this.route);
}
