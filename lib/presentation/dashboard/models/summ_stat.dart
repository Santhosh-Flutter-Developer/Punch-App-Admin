import 'package:flutter/material.dart';

class SummStat {
  final String label;
  final int count;
  final IconData icon;
  final Color color;
  const SummStat(this.label, this.count, this.icon, this.color);
}
